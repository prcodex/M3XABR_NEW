# Expected Outputs

Reserved for golden regression outputs.

Populate this directory as you validate good responses on real
corpus + real LLM. Each file should be a JSON dump of a
`PipelineResult` for a specific query, suitable for regression tests.

Suggested naming: `<query_file_name>_expected.json`.

Empty in v0 — add as you go.
